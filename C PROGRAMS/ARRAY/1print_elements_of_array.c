#include <stdio.h>
#include <conio.h>
int main()
{
    int a[10], i;
    for (i = 0; i < 10; i++)
    {
        printf("Enter a number :");
        scanf("%d", &a[i]);
    }
    printf("The elements of array a are :");
    for (i = 0; i < 10; i++)
    {
        if(a[i]==0);
    }
        printf("\n%d", a[i]);

    return 0;
}